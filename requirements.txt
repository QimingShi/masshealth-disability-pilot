# MassHealth Disability Reviewer Assistant — pipeline runtime
#
# All LLM + embedding calls go through Amazon Bedrock (boto3). All retrieval
# happens in Postgres via pgvector. There is no local-model dependency.

# AWS — Textract OCR, Bedrock embeddings (Titan v2) and LLM (Claude Opus 4.6)
boto3>=1.34

# Postgres + pgvector adapter
psycopg2-binary>=2.9
pgvector>=0.2

# SSA listings loader uses SQLAlchemy text() for parameterized statements
SQLAlchemy>=2.0

# PDF rendering + bbox-highlight annotation (pipeline/annotate_pdf.py)
pymupdf>=1.23

# Numeric arrays (pgvector adapter returns numpy arrays; embedding workers
# batch through numpy)
numpy>=1.24

# Word-doc renderer (pipeline/output_docx.py) — fills SSA Blue Book templates
# under templates/listings/<code> *.docx with per-leaf verdicts + AI narrative
python-docx>=1.1
